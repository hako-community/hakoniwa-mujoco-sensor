[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "hakoniwa-sensor-health"
version = "1.0.0"
description = "Application-owned SensorHealth codec over standard Hakoniwa PDUs"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["python"]
